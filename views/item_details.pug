extends layout

block content
  p= item.name 
  p= item.description 
  p= item.category 
  p= item.price 
  p= item.number_in_stock
  p
    a(href=item.url+'/delete') Delete item
  p
    a(href=item.url+'/update') Update item